#!/usr/bin/env ζ₂

acorn = require('./acorn') // https://github.com/marijnh/acorn

// q = acorn.parse(fs('corpus/underscore.js').$)

corpus = fs('corpus/all').find('>').map(λ(v){↩ 'corpus/all/'+v})
corpus = corpus.map(λ(v){↩ [v, _.memoize(λ(){↩ fs_(v).$})]})

ζ2_repl_start()
