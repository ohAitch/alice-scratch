#!/usr/bin/env ζ₂
util ← require('util')
child_process ← require('child_process')

// ---------------------------------- util ---------------------------------- //

argv ← process.argv.slice(2)
stdout ← λ(ι){process.stdout.write(ι)}
remove_ansi_colors ← λ(ι){↩ ι.replace(/\[.+?m/g,'')}
diy_stdin ← λ(f){process.stdin.setRawMode(true); process.stdin.resume().setEncoding('utf8').on('data',f)}
IS_URL ← /^((https?|file):\/\/|mailto:)/.λ
FIND_URL ← /\b((https?|file):[⟩\/][⟩\/](?:\\ |\S)+|mailto:[^\s\\@]+@[^\s\\@]+)/.λ
postredirect_url ← λ(ι){↩ ι
	.replace(/^http:\/\/google.com\//,'http://www.google.com/')
	.replace(/^http:/,'https:') }
as_url ← λ(ι){t←;
	↩ IS_URL(ι)? encodeURI(ι) :
	(t=FIND_URL(ι))? encodeURI(t[1].replace(/⟩/g,'/').replace(/\\ /g,' ')) :
		"http://google.com/search?q="+encodeURIComponent(ι) }
height ← λ(ι){↩ remove_ansi_colors(ι).split('\n').map(λ(ι){↩ 1+floor(ι.length/80)}).reduce(λ(a,b){↩a+b},0)}

osa←; osa_v←; (λ(){
	osa_gen ← λ(ι){↩ "osascript -ss "+ι.split(/; */).map(λ(ι){↩ "-e "+bash_encode(ι)}).join(" ")}
	osa_parse_array ← λ(ι){↩ JSON.parse(ι.split(/("(?:\\.|.)*?")/g).map(λ(ι){↩ /^"/.λ(ι)? ι : ι.replace(/\{/g,'[').replace(/\}/g,']')}).join(''))} // based on https://github.com/FWeinb/node-osascript/blob/master/lib/osa-parser.peg
	osa = λ(ι){child_process.exec(osa_gen(ι))}
	osa_v = λ(ι){↩ osa_parse_array(child_process.execSync(osa_gen(ι))+'')}
	})()
bash ←   λ(flag,ι){!ι && (ι = flag, flag = ''); child_process.exec("bash -c "+flag+" "+bash_encode(ι))}
bash_v ← λ(flag,ι){!ι && (ι = flag, flag = ''); ↩ (child_process.execSync("bash -c "+flag+" "+bash_encode(ι))+'').replace(/\n$/,'')}

bash_encode ← λ(){↩ ζ2_A(arguments).map(λ(ι){↩ ι.replace(/\W/g,"\\$&")}).join(" ")}
basename_encode ← λ(ι){↩ ι.replace(/\//g,'⟩')}
osa_encode ← λ(ι){↩ '"'+ι.replace(/["\\]/g,'\\$&')+'"'}

// -------------------------------------------------------------------------- //

PROMPT ← '[30;47m>[0m '
PREVIEW_PROMPT ← '[30;47m:[0m'
PREVIEW_MORE_PROMPT ← '[30;47m.[0m [30;47m.[0m [30;47m.[0m'
DATA_FILE ← '~/ali/github/scratch/prentice/~:.prentice'

D ← { // data
	line:'', pos:0,
	preview:null,
	}
print ← λ(){a ← ζ2_A(arguments); stdout('[0G[2K'+(a.length <= 1 && a[0] == null? '' : sprint(a)+'\n')+PROMPT+D.line); pos_set()}
hide_interface ← λ(){stdout(D.preview === null? '[0G[2K' : '['+(height(D.preview)+height(PROMPT+D.line)-1)+'A[0G[J')}
show_interface ← λ(){print(D.preview); pos_set()}
preview ← λ(ι){
	if (ι === null) t ← null
	else if (height(ι) > 20) {t ← ι.split('\n'); r ← []; while (height(r.join('\n')) <= 20) r.push(t.pop()); r.pop(); t ← PREVIEW_MORE_PROMPT+'\n'+r.reverse().join('\n')}
	else t ← PREVIEW_PROMPT+'\n'+ι
	D.preview === t || (hide_interface(), D.preview = t, show_interface())
	}
pos_set ← λ(ι){arguments.length === 1 && (D.pos = ι); stdout('['+(1+D.pos+remove_ansi_colors(PROMPT).length)+'G')}
line_splice ← λ(i,del,ι){D.pos === i || pos_set(i); no_set ← D.pos === D.line.length; D.line = D.line.slice(0,D.pos)+ι+D.line.slice(D.pos+del); stdout((del-ι.length>0?'[K':'')+D.line.slice(D.pos)); D.pos += ι.length; no_set || pos_set()}

on_key ← λ λ(key){line_a ← D.line; switch (key) {default:
	/^[ -~±-³¹ʰ-ʸˡ-ˣΑ-ΡΣ-ωᴬ-ᵛᵢ-ᵥᶜᶠᶻ⁰ⁱ⁴-₎ₐ-ₓₕ-ₜ↩]+$/.λ(key)?
		line_splice(D.pos,0,key) :
		print('got '+util.inspect(key).replace(/\\u00/g,'\\x'))
	break; case '\x03': hide_interface(); process.stdin.pause()
	break; case '\r': preview(null); D.line === '' && (stdout('<reboot>\n\n'), process.exit(44)); D.line = ''; D.pos = 0; stdout('\n'+PROMPT+D.line); on_line_change(line_a,true); ↩
	break; case '\x7f': D.pos > 0 && line_splice(D.pos-1,1,'')
	break; case '[D': D.pos > 0 && pos_set(D.pos-1)
	break; case '[C': D.pos < D.line.length && pos_set(D.pos+1)
	break; case '\x01': pos_set(0)
	break; case '\x05': pos_set(D.line.length)
	break; case '\x15': line_splice(0,D.pos,'')
	break; case '\t':
		ι ← D.line.slice(0,D.pos).match(/\S*$/)[0]
		if (ι === '') {λ(D.line.length%'  '.length===0?'  ':' '); break}
		if (D.pos !== D.line.length) {print('tab completion is scary if not at the end of a line'); break}
		//! potentially should preview
		completions ← /* if is filename context */ fs('.').find('>').map(λ(ι){↩ ι.slice(2)})
		if (D.line === ι) completions.push('ls','chrome','echo','dl')
		completions = completions.filter(λ(t){↩ t.slice(0,ι.length) === ι})
		if (completions.length > 1) {print('found',completions.length,'completions'); break}
		if (completions.length === 0) {print('no completions found'); break}
		line_splice(D.pos - ι.length, ι.length, completions[0])
	}; line_a === D.line || on_line_change(D.line) }

on_line_change ← λ(ι,act){ιs ← ι.trim().split(' ')
	P ← λ(ι){P = null; preview(ι)}
	if (ιs[0] === 'dl') {
		t ← ιs.slice(1).join(' ')
		if (/^http:\/\/youtu\.be\/[^?#]+$/.λ(t)) {
			if (!act) {
				P('would download 1 mp3 from youtube')
			} else {
				t = 'cd ~/Downloads; youtube-dl --extract-audio --audio-format mp3 -o '+bash_encode(basename_encode('%(title)s '+t+' .%(ext)s'))+' '+bash_encode(t)
				osa('tell application "terminal"; do script '+osa_encode(t)+'; end tell'); osa('tell application "terminal" to activate')
			}
		}
	} else if (ιs.length === 1 && (ιs[0] === 'l' || ιs[0] === 'ls')) {
		;(act? print : P)(bash_v("-i",'cd '+bash_encode(process.cwd())+'; echo "$green$(this)$reset"; CLICOLOR_FORCE=t l'))
	} else if (ιs[0] === 'chrome') {
		t ← ιs.length === 1? 'chrome:newtab' : as_url(ιs.slice(1).join(' '))
		!act?
			P(t) :
			osa('tell application "chrome" to open location '+osa_encode(postredirect_url(t)))
	} else if (ιs[0] === 'echo') {
		t ← ιs.slice(1).join(' ')
		;(act? print : P)(t)
	}
	P&&P(null)
	}

if (argv.length) {print = global.print; preview = null; on_line_change(argv.join(' '),true)}
else {
	t ← fs(DATA_FILE).$; t && _.extend(D, _(JSON.parse(t)).pick(_.keys(D)))
	process.on('exit',λ(){fs(DATA_FILE).$ = JSON.stringify(D,null,'  ')})
	show_interface(); diy_stdin(on_key)
	}

// mv E\t Fate Stay:Night - Emiya#0.mp3
// crop F\t

// http://tampermonkey.net/faq.php
// chrome-extension://dhdgffkkebhmkfjojejmpbldmpobfkfo/options.html#open=1bbe177e-96ec-49c8-8086-f3bf7a2a9e58
// osa_v('tell application "chrome"; get {URL,title} of tabs of windows; end tell')

//! line wrapping doesn't work
// multiline input (literal \n if the last line seemed incomplete)
