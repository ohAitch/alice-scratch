#!/usr/bin/env node
var fs = require("fs");
var path = require("path");
var ξ = require("./ξ");
var η = require("./η");
var φ = require("./φ");
if ((process["argv"][1]===__filename)) {var in_ = process["argv"]["slice"](2,Number("-1"));
	var out = process["argv"]["slice"](Number("-1"))[0];
	in_["map"](function(in_) {{fs["writeFileSync"](((out+"/")+path["basename"](in_,".ζ₁")),ξ(η(φ((fs["readFileSync"](in_)+" ")))))}})}
	else ""